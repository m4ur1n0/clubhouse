<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= content_for(:title) || "Clubhouse" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="theme-color" content="#4E2A84">
    
    <!-- PWA support -->
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
    <meta name="apple-mobile-web-app-title" content="Clubhouse">
    
    <!-- iOS splash screen images -->
    <link rel="apple-touch-startup-image" href="/splash.png">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <!-- Web Manifest -->
    <link rel="manifest" href="/manifest.webmanifest">
    
    <!-- Icons -->
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-northwestern-purple">
      <div class="container">
        <%= link_to root_path, class: "navbar-brand" do %>
            <h1 class="mb-0">Clubhouse</h1>
        <% end %>
        
        <div class="navbar-nav ms-auto">
          <% if user_signed_in? %>
            <span class="navbar-text me-3">
              Welcome, <%= current_user.name %>!
            </span>
            <%= link_to "Logout", logout_path, method: :get, class: "btn btn-outline-light" %>
          <% else %>
            <%= link_to "Sign In", signin_path, class: "btn btn-outline-light me-2" %>
            <%= link_to "Sign Up", signup_path, class: "btn btn-light" %>
          <% end %>
        </div>
      </div>
    </nav>

    <div class="app-container">
        <% if notice %>
            <div class="alert alert-success mt-3"><%= notice %></div>
        <% end %>
        <% if alert %>
            <div class="alert alert-danger mt-3"><%= alert %></div>
        <% end %>

        <%= yield %>
    </div>

    <footer class="footer mt-auto py-4 bg-northwestern-purple">
      <div class="container">
        <div class="row">
          <div class="col-md-6 text-white">
            <h5 class="fw-bold">Clubhouse</h5>
            <p class="mb-0">Discover different clubs at Northwestern University</p>
          </div>
        </div>
      </div>
    </footer>

    <% if Rails.env.production? %>
      <script>
        if ('serviceWorker' in navigator) {
          window.addEventListener('load', () => {
            navigator.serviceWorker.register('/service-worker.js')
              .then(registration => {
                console.log('Service Worker registered successfully');
              })
              .catch(error => {
                console.log('Service Worker registration failed:', error);
              });
          });
        }
      </script>
    <% end %>
  </body>
</html>
